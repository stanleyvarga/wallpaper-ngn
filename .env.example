# .env.example - example configuration for wp-ngn
# Copy this to .env and edit paths if needed:

WALLPAPERS_DIR="$HOME/Wallpapers"
CONFIG_DIR="$HOME/.wallpaper-ngn"
CONFIG_FILE="$CONFIG_DIR/config.json"

# DEBUG mode (true/false)
DEBUG=false

# You can override these env vars instead of editing .env, e.g.:
# WALLPAPERS_DIR=/path/to/my/wallpapers CONFIG_DIR=/path/to/config wp-ngn --random

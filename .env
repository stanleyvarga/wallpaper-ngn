# .env - configuration for wp-ngn
# This file is sourced by the `wp-ngn` script. Edit values if you want to override defaults.
# Paths may use $HOME which will be expanded when the file is sourced.

WALLPAPERS_DIR="$HOME/Wallpapers"
CONFIG_DIR="$HOME/.wallpaper-ngn"
CONFIG_FILE="$CONFIG_DIR/config.json"

# Optional: set to 'true' for extra debug output from the script (script must respect this)
DEBUG=false
